Generate and open the Allure report from the last test run.

```bash
mvn allure:serve
```

To generate only (no browser):

```bash
mvn allure:report
# report written to target/site/allure-maven-plugin/index.html
```
